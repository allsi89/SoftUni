function solve(word1, word2, word3) {
    let totalLength = word1.length + word2.length + word3.length;
    console.log(totalLength);
    let average = parseInt(totalLength / 3);
    console.log(average);
}

solve('pasta', '5', '22.3');